import React from 'react';

import Drawer from '@material-ui/core/Drawer';

const SideBar = (props) => {
  return (
    <Drawer
      variant="permanent"
      anchor="left">
    </Drawer>
  )
};

export default SideBar;